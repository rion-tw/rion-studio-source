{
  "$schema": "https://unpkg.com/knip@latest/schema.json",
  "entry": [
    "src/renderer/runtime-shell/runtimeDivider.ts",
    "src/renderer/runtime-shell/runtimeTabStrip.ts",
    "src/shared/browser-overlay/*.js",
    "scripts/*.mjs",
    "tests/**/*.{ts,tsx}"
  ],
  "project": [
    "src/**/*.{ts,tsx,js}",
    "scripts/*.{mjs,mts}",
    "tests/**/*.{ts,tsx}",
    "*.{ts,mjs}"
  ],
  "ignore": [
    "scripts/*.d.mts",
    "src/renderer/src/global.d.ts",
    "src/shared/generated/**",
    // Raw runtime fragments are native Rust include_str! inputs and are not standalone JavaScript modules.
    "src/shared/browser-overlay/macro-overlay-runtime/*.js"
  ],
  "ignoreDependencies": [
    // Executed through pnpm in the Tauri lifecycle scripts.
    "@tauri-apps/cli",
    // Loaded from CSS through @import, outside Knip's JavaScript dependency graph.
    "tailwindcss"
  ]
}
