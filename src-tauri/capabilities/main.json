{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local-only",
  "description": "Minimum local capability for the bundled Rion Studio renderer.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
