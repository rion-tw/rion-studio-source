# Renderer Scope

- Keep code browser-safe and use only the typed `window.rionStudio` API.
- Route components coordinate feature modules; move large controls, dialogs, and
  formatters into cohesive neighboring files.
- Reuse local UI primitives, Tailwind tokens, and four-locale translations.
- Maintain usable layout at 960x640.
